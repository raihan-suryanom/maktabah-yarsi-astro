import { useEffect, useState } from "react";
import Select from 'react-select'

const Test = () => {
  const [first, setfirst] = useState([]);
  return <div>
    <p>Text</p>
    <Select isMulti/>
  </div>;
};

export default Test;
