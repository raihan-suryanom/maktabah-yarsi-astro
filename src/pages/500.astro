---
interface Props {
  error: unknown;
}

const { error } = Astro.props;
---

<div>
  {
    error instanceof Error
      ? error.message
      : "Terjadi suatu kesalahan atau internet Anda sedang mati!"
  }
</div>
